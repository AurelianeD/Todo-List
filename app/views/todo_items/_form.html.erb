<%= form_for([@todo_list, @todo_list.todo_items.build]) do |f| %>
  <%= f.text_field :content, placeholder: "Liste tes tâches afin de t'organiser plus facilement !" %>
  <%= f.submit "Ajouter"%>
 <% end %>
